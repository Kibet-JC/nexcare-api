// Placeholder so the TypeScript toolchain has an input to compile against while
// the repo has no application code. The real entrypoint (Express app, routing,
// etc.) lands in Issue #3 and will replace this file.
export {};
